{
  "name": "flixate",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "phase0": "node --env-file-if-exists=.env --import tsx scripts/phase0.ts",
    "phase0:sample": "npm run phase0 -- --sample",
    "benchmark:browser": "node --import tsx scripts/browser-benchmark.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "playwright-core": "^1.62.1",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
